<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thingstoseeinaichiprefecture">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThingsToSeeInAichiPrefecture">
        <activity
            android:name=".TokonameActivity"
            android:label="Tokoname"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".InuyamaActivity"
            android:label="Inuyama"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".GamagoriActivity"
            android:label="Gamagori"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".OkazakiActivity"
            android:label="Okazaki"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>